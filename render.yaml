services:
  - type: web
    name: cerberus-command-center
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn web_server:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /api/dashboard
    autoDeploy: true
